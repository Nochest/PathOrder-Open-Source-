@model IEnumerable<PathOrder.Models.FacturaMercaderia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Factura.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mercaderia.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Factura.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mercaderia.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdFacturaMercaderia }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdFacturaMercaderia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdFacturaMercaderia })
        </td>
    </tr>
}

</table>
