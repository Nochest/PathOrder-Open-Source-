@model IEnumerable<PathOrder.Models.Bulto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Peso)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrdenDespacho.Prioridad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Peso)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrdenDespacho.Prioridad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdBulto }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdBulto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdBulto })
        </td>
    </tr>
}

</table>
